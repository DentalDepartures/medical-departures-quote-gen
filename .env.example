# Anthropic API key — used by the Vercel serverless function (api/extract.ts)
# Agents never see this key; it stays server-side.
# Copy this file to .env.local for local development with `vercel dev`.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model used for extraction
# ANTHROPIC_MODEL=claude-opus-4-5
